import { InputWidthDirective } from './input-width.directive';

describe('InputWidthDirective', () => {
  it('should create an instance', () => {
    const directive = new InputWidthDirective();
    expect(directive).toBeTruthy();
  });
});
